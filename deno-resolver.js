const interfaceVersion = 2;

/**
 *
 * @param source
 * @param file
 * @param config
 * @example
 */
const resolve = (source, file, config) => {
	console.log("TEST");
};

export {
	interfaceVersion,
	resolve
};
